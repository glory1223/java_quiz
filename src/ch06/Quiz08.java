package ch06;

public class Quiz08 {
    /*
    ➊ 정적 멤버는 static으로 선언된 필드와 메소드를 말한다.
    ➋ 인스턴스 필드는 생성자 및 정적 블록에서 초기화될 수 있다. -> 인스턴스필드는 생성자에서 초기화되며 정적필드는 정적블록에서 초기화된다.
    ➌ 정적 필드와 정적 메소드는 객체 생성 없이 클래스를 통해 접근할 수 있다.
    ➍ 인스턴스 필드와 메소드는 객체를 생성하고 사용해야 한다.
     */
}
